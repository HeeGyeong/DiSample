package com.example.disample.dagger

import com.example.disample.view.KoinActivity
import dagger.Component

/*
@Component(modules = [TestModule::class])
interface TestComponent {
    fun inject(activity: KoinActivity)
}*/
